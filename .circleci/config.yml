version: 2

executors:
  node:
    docker:
      - image: circleci/node:10
    working_directory: ~/code

jobs:
  build:
    executor: node
    steps:
      - checkout
      - run: yarn
      - persist_to_workspace:
          root: ~/code
          paths:
            - .
  test:
    executor: node
    steps:
      - attach_workspace:
          at: ~/code
      - run: yarn ci-test

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test
