version: 2

executors:
  nodejs:
    docker:
      - image: circleci/node:10
    working_directory: ~/code

jobs:
  build:
    executor: nodejs
    steps:
      - checkout
      - run: yarn
      - persist_to_workspace:
          root: ~/code
          paths:
            - .
  test:
    executor: nodejs
    steps:
      - attach_workspace:
          at: ~/code
      - run: yarn ci-test

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test
